[build]
  command = "npm run build"
  publish = "dist"
  functions = "server"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["@replit/vite-plugin-cartographer", "pg-native", "@babel/preset-typescript"]
