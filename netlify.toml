[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "server"
  node_bundler = "esbuild"
  node_version = "18"
  external_node_modules = ["@replit/vite-plugin-cartographer", "pg-native", "@babel/preset-typescript", "lightningcss"]

[build.environment]
  NODE_VERSION = "18"
